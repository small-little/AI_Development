## 1. テキスト表示コンポーネント

用途： 文字やタイトル、説明文などをアプリに表示するためのコンポーネント。

### 主なウィジェットと例
ウィジェット	用途	説明
st.title()	タイトル表示	ページ上部に大きなタイトルを表示
st.header()	セクション見出し	中タイトルを表示
st.markdown()	Markdown形式で表示	太字・リスト・リンクなどを装飾可能
コード例
import streamlit as st

タイトル（大見出し）
st.title(" Streamlit テキスト表示コンポーネント")

ヘッダー（中見出し）
st.header("これは header() です")

マークダウン形式で表示
st.markdown("""
### Markdownによる装飾例
- **太字**
- *斜体*
- [GitHubへのリンク](https://github.com)
""")
## 2. データ表示コンポーネント

用途： データフレームや数値データを表やグラフとして表示。

### 主なウィジェット
ウィジェット	用途	説明
st.dataframe()	データを表形式で表示（ソート可能）	Pandas DataFrameなどを動的に表示
st.table()	静的な表として表示	編集やスクロールなしの単純表示
st.line_chart()	折れ線グラフを描画	数値データの可視化に便利
コード例
import streamlit as st
import pandas as pd
import numpy as np

st.title("データ表示コンポーネント")

サンプルデータ作成
df = pd.DataFrame({
    "月": ["1月", "2月", "3月", "4月"],
    "売上": [100, 150, 200, 180]
})

動的な表（スクロール可能）
st.dataframe(df)

静的な表
st.table(df)

折れ線グラフ
st.line_chart(df["売上"])

## 3. 入力ウィジェット（インタラクティブコンポーネント）

用途： ユーザーが入力・操作できる要素。

### 主なウィジェット
ウィジェット	用途	説明
st.text_input()	テキスト入力欄	ユーザーから文字列を取得
st.slider()	スライダー入力	数値や範囲を指定
st.selectbox()	プルダウンメニュー	選択肢から選ばせる
コード例
import streamlit as st

st.title("入力ウィジェット（インタラクティブコンポーネント）")

テキスト入力
name = st.text_input("あなたの名前を入力してください")

スライダー入力
age = st.slider("年齢を選んでください", 0, 100, 25)

セレクトボックス
color = st.selectbox("好きな色を選んでください", ["赤", "青", "緑"])

入力結果を表示
st.write(f"こんにちは、{name} さん！年齢：{age}歳、好きな色：{color}")

## 4. レイアウトコンポーネント

用途： ページの構造を整えたり、複数の要素を並べる。

### 主なウィジェット
ウィジェット	用途	説明
st.sidebar	サイドバーに要素を配置	設定メニューやフィルタ用
st.columns()	カラム（列）を並べる	横並びに表示
st.tabs()	タブでコンテンツを切り替え	複数ページのように表示可能
コード例
import streamlit as st

st.title(" レイアウトコンポーネント")

サイドバーに入力要素
with st.sidebar:
    st.header("設定メニュー")
    option = st.selectbox("テーマを選択", ["ライト", "ダーク"])

カラムレイアウト
col1, col2, col3 = st.columns(3)
col1.write("左カラム")
col2.write("中央カラム")
col3.write("右カラム")

タブレイアウト
tab1, tab2 = st.tabs(["Tab 1", "Tab 2"])
with tab1:
    st.write("これはタブ1の内容です。")
with tab2:
    st.write("これはタブ2の内容です。")

## 5. ステータス表示コンポーネント

用途： 処理の進行状況や状態をユーザーに伝える。

### 主なウィジェット
ウィジェット	用途	説明
st.progress()	進捗バーを表示	処理の進行状況を示す
st.spinner()	処理中インジケータ	一時的な「読み込み中」表示
st.success() / st.error() / st.warning()	メッセージ表示	成功・警告・エラー状態を通知
コード例
import streamlit as st
import time

st.title("⚙️ ステータス表示コンポーネント")

プログレスバー
progress = st.progress(0)
for i in range(100):
    time.sleep(0.02)
    progress.progress(i + 1)

スピナー表示
with st.spinner("処理中です..."):
    time.sleep(2)
st.success("完了しました！")

状態メッセージ
st.warning("注意が必要です。")
st.error("エラーが発生しました！")
st.info("情報: Streamlitは便利です。")


